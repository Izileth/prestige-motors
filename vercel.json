{
    "version": 2,
    "buildCommand": "npm run build",
    "outputDirectory": "build",
    "framework": "vite",
    "routes": [
        { "handle": "filesystem" },
        { "app": "/.*", "dest": "/index.tsx" }
    ]
}